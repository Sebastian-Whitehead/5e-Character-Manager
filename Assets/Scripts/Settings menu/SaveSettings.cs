using UnityEngine;

namespace Settings_menu
{
    
    //TODO: Implement saving and loading of settings
    public class SaveSettings : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
